enabled: true

description: 'A chance to add slowness de-buff your target'

display-name: 'Entangle'

lore-display: '&7Entangle %level%'

max-level: 3

enchant-type: 'BOW'

# Cost per level in XP.
cost:
  expression: '25 * %level%'

chance:
  expression: '%level% * 0.4'

potion-level:
  expression: '%level%'

potion-duration:
  expression: '%level% * 3'