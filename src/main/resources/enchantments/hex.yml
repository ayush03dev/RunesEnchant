enabled: true

description: 'A chance of inflicting blindness on your victim'

lore-display: '&7Hex %level%'

display-name: 'Hex'

max-level: 3

enchant-type: 'SWORD'

# Cost per level in XP.
cost:
  expression: '25 * %level%'

chance:
  expression: '(%level% + 1) * 2'
  level_1: 15

potion-level:
  expression: '%level%'

potion-duration:
  expression: '(%level% + 1) * 5'